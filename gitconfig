[user]
	name        = William
        email       = william@undefined.re
	signingkey  = william@undefined.re

[color]
	ui = true

[alias]
	st   = status -s
	ci   = commit -a
	cm   = commit -m
	cim  = commit -a -m
	b    = branch
	ba   = branch -a
	bt   = branch --track
	co   = checkout
	cob  = checkout -b
	cot  = checkout --track
	d    = diff
	dw   = diff --word-diff
	dr   = diff --raw
	ds   = diff --stat
	l    = log --decorate=short --graph
	lp   = log -p --decorate=short
	lw   = log -p --word-diff --decorate=short
	ls   = log --stat --decorate=short
	lr   = log --raw --decorate=short
	lg   = log --graph --decorate=short
	lo   = log --pretty=oneline --decorate=short
	f    = fetch
	ph   = push
	pl   = pull
#	r    = remote
	rs   = remote show
	ra   = remote add
	rd   = remote del
	lsf  = ls-files
	m    = merge
	cl   = clean -fdx
	t    = tag
	ui   = update-index

[merge]
        tool   = vimdiff

[mergetool]
        prompt = true

[mergetool "vimdiff"]
        cmd    = nvim -d $BASE $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'

[rerere]
	enabled = 1

[diff]
	renamelimit = 5000

[commit]
	gpgsign = true
